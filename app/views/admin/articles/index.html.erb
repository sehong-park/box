<% provide(:title, '관리자모드 | 질문') %>
<div class="row" id="admin">
  <%= render 'admin/shared/aside' %>

  <div class="col-sm-10">
    <%= render 'articles/qna', {questions: @questions} %>
  </div>

</div>