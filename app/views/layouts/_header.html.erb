<header class="navbar">
  <div class="navbar-inner">
    <div class="container-fluid">
      <%= link_to "I am Box", root_path, id: "logo" %>
      <nav>
        <ul class="nav nav-pills pull-right">
          <li><%= link_to "서비스신청", ordering_path %></li>
          <% if signed_in? %>
            <li><%= link_to "사용자", users_path %></li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                내정보 <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "이용내역", user_path(current_user) %></li>
                <li><%= link_to "정보수정", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "로그아웃", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "로그인", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
