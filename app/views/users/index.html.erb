<% provide(:title, '회원목록') %>
<div class="row">
  <div class="col-sm-10">
    <h1 class="text-center">회원 목록</h1>
    <ul>
      <% @users.each do |user| %>
      <li><%= user.name %></li>
      <% end %>
    </ul>
  </div>
</div>
